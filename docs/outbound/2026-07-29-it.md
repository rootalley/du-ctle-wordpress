# Outbound — DU IT, 2026-07-29

**To:** Ellen Alamilla <ealamilla@dom.edu> — acting IT Project Manager for this project
**Cc:** *(none yet — add the identity and messaging technical contacts once IT names them; that request is item 4 below)*
**Subject:** CTLE WordPress — our side is complete; checking in on the Entra app (IT-1) and the ctle-noreply mailbox (IT-2)

---

Hi Ellen,

Following up on our July 27 meeting. Short version: **everything on the CTLE side that doesn't depend on IT is now finished**, so the project's remaining path runs entirely through the two requests we submitted on the 27th. I'd like a status read and, more importantly, a turnaround estimate.

## Where the site stands

The WordPress build is done through the infrastructure layer:

- Live and serving at https://ctle.dom.edu over HTTPS, with all other hostnames redirecting to it
- PHP 8.4, clear of the December 2026 security-support cutoff that applied to our previous version
- Security stack in place: audit logging, an obfuscated login path, XML-RPC disabled, open registration off, CDN and edge image optimization enabled
- **No password-authenticated account exists on the site at all.** The temporary provisioning account was deleted on the 27th. Administrators enter through the hosting console's passwordless auto-login; faculty will enter through Entra. There is no local password login left to attack.
- Daily backups plus point-in-time restore, both verified
- Site is deliberately hidden from search engines until launch

## Thank you — these are closed on your side

- DNS ticket 26363781 — A record and www CNAME delivered, certificates issued, site live. Nothing further needed.
- Kinsta DPA — executed.
- Vendor security review — approved.
- Security sign-off on the replacement administrator protection model (IT-6) — received July 27.

## One thing we withdrew, which reduces integration scope

We have **dropped the Canvas LTI 1.3 integration entirely.** CTLE is a standalone site that needs none of LTI Advantage's services — no grade passback, no roster sync, no deep linking — and the Entra faculty group already gates access regardless of how someone reaches the site. Faculty will instead click the existing CTLE button in the Canvas global navigation, which we're retargeting to the site's SSO-initiation URL. Since Canvas is on the same Entra tenant, that click completes sign-in silently.

This removes a whole workstream and de-risks the timeline. It also means **Entra SSO is now the single launch integration** — which raises the stakes on IT-1 rather than lowering them.

## What I need from IT

**1. IT-1 — Entra ID app registration (Request 1). Status, and a realistic turnaround estimate.**

The estimate is the more urgent of the two. Our Director has to decide what ships in August, and she cannot make that call without knowing whether a working Entra app registration is realistic before the end of the month. **If you can give me even a rough range by Friday, July 31, that unblocks the decision.** If the estimate itself needs more time, a date by which you'll have it works just as well — I'd rather have an honest "mid-September" than an optimistic silence.

SSO gates faculty sign-in, administrator elevation, the discussion forums, the Canvas launch link, and roughly half of our pre-launch verification.

**2. IT-2 — `ctle-noreply@dom.edu` mailbox + Graph `Mail.Send` (Request 2). Status.**

Until this lands, WordPress has no working mail transport. Our security alerting on administrator logins and role changes is built and running but cannot deliver, and event registration confirmations and reminders can't be built or tested.

**3. IT-4 — the `ctle@dom.edu` access list.** This was left open on the 27th and was due July 31. That mailbox now receives the MyKinsta two-factor codes gating WordPress Administrator access, so its access list is a security control. I need to know who currently has access and whether IT wants it narrowed.

**4. A named technical contact for each of the two requests**, so configuration questions don't route through the ticket queue.

## Three questions that would save us a round trip

1. **OIDC or SAML?** We prefer OIDC — the Graph mail integration is OAuth-based, so one protocol keeps a single troubleshooting model — but we'll implement whichever DU standardizes on. We need to know before we choose the WordPress plugin.

2. **Does adding or amending a redirect URI later require a new ticket?** If yes, tell us now and we'll hold Request 1 until the plugin is installed and we can hand you the exact string, rather than burning a second ticket. If a later amendment is routine, please proceed with what we submitted.

3. **Is `employeeId` reliably populated for all faculty in the directory?** It's the account primary key in our design — it preserves record continuity across name and email changes. If coverage is partial, we'll key accounts on `oid` instead and treat `employeeId` as secondary. This is much cheaper to know now than after accounts exist.

## What happens the moment each one lands

- **IT-1** → we install and configure the SSO plugin the same week, test with the DU test account you provide, then hand DU Learning Technologies the SSO-initiation URL for the Canvas button. The Canvas-side gating work is already built and tested, so only the URL is missing.
- **IT-2** → mail is configured and tested in under a day; it's a credential entry, not a build.

Both requests are specified in full in the document I sent on the 27th, including exactly what we need returned — tenant and client IDs, the client secret **with its expiry date and rotation owner**, the discovery URL, claim names, and a test account.

Happy to jump on a call if that's faster than email.

Thanks,
Steven

---

*Steven Endres · Learning Technologies · Dominican University*

---

## Notes for the sender (delete before sending)

- Ellen Alamilla is acting as IT PM for this project, so this is a single-recipient send — there is no separate PM to Cc.
- The single most important sentence is the Friday-July-31 ask for an estimate. If nothing else comes back, that does.
- If IT-1 shows no movement by ~August 7, escalate: the estimate is what determines whether CD-6 becomes "public layer only in August" or "slip the launch."
- Tracked as IT-1, IT-2, IT-4 in `STATUS_AND_ACTIONS.md`; requests are `IT_REQUESTS.md` Requests 1 and 2.
